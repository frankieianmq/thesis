<?xml version="1.0" encoding="UTF-8"?>
<!-- generated by: Y. C. Lee -->
<config randomSeed="1024"> 
  <!-- define server types 
  Min memory per core is 200MB and the total memory capacity should be the min of 1000MB so as min disk per core -->
  
  <servers>
	<server type="juju" limit="2" bootupTime="60" hourlyRate="0.2" coreCount="2" memory="4000" disk="16000" />
	<server type="joon" limit="2" bootupTime="60" hourlyRate="0.4" coreCount="4" memory="16000" disk="64000" />
	<server type="silk" limit="1" bootupTime="80" hourlyRate="0.8" coreCount="8" memory="32000" disk="256000" />
  </servers>
  <!-- define job types -->
  <!-- the sum of rate values must be 100 (100%) -->
  <!-- minRunTime and maxRunTime must be 1 or greater bounded by INT_MAX -->
  <jobs file="ds-jobs.xml"/>
  <workload type="moderate" minLoad="30" maxLoad="70" />
  <termination>
	<!-- simulation terminates whichever condition meets first -->
	<!-- 60 seconds * 60 minutes * 24 hours * 7 days = 604800 seconds -->
	<condition type="endtime" value="604800" />
	<condition type="jobcount" value="10" />
  </termination>
</config> 
